name: Generate ASCII Artwork

on:
  push:


jobs:
  ascii-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: list repo File Exists 
        run: ls -ltra        

      - name: Executing script 
        run: ./ascii-script.sh